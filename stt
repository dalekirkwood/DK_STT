#!/bin/bash
pid=$(cat /tmp/stt-app.pid 2>/dev/null)
if [ -z "$pid" ] || ! kill -0 "$pid" 2>/dev/null; then
    notify-send "STT" "App not running — start stt-app first" -i dialog-error
    exit 1
fi
kill -USR1 "$pid"
